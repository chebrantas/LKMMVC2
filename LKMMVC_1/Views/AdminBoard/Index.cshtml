@model IEnumerable<LKMMVC_1.Models.ManagementBiography>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ManagementPersonName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Info)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ManagementPersonName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Info)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.ManagementBiographyID }) |
                @Html.ActionLink("Details", "Details", new { id=item.ManagementBiographyID }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.ManagementBiographyID })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
