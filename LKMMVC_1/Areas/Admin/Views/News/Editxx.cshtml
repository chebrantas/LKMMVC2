@model IEnumerable<LKMMVC_1.Models.NewsPhoto>

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NewsID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PhotoLocation)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NewsID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PhotoLocation)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.NewsPhotoID }) |
            @Html.ActionLink("Details", "Details", new { id=item.NewsPhotoID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.NewsPhotoID })
        </td>
    </tr>
}

</table>
